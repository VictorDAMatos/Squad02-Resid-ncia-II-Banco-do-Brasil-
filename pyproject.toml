[project]
name = "squad02-banco-brasil-risco-auditoria"
version = "2.1.0"
description = "API Banco do Brasil - Core bancário, detecção de fraudes, análise manual, auditoria e front-end integrado."
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "pydantic>=2.8.0",
    "scikit-learn>=1.5.0",
    "pandas>=2.2.0",
    "numpy>=1.26.0"
]
